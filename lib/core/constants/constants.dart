/*
import 'package:dio/dio.dart';
import 'package:intl/intl.dart';

const String appAPIBaseURL = 'http://10.0.2.2:8000/api/v1/';
const connectTimeout =  Duration(seconds: 15);
const receiveTimeout = Duration(seconds: 13);
const responseType = ResponseType.json;
const contentTypeHeader = 'application/json';
bool validateStatus(int? status) {
  return status! < 500;
}


final DateFormat dateFormat = DateFormat('yyyy-MM-ddTHH:mm:ss.SSSSSS');*/
